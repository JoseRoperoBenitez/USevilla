function PbCauchy

f = @(t,y)5*y;
[tt,yy] = ode45 (f,[0,1], 1);

exacta = exp(5*tt);
close all

subplot (2,1,1) %2 columnas primera de ellas
plot (tt,yy, 'ro-' ,tt,exacta, 'b+')
legend ('aprox', 'exacta')
subplot (2,1,2

hold on 
plot ()

